# URL del Web App de Google Apps Script (deploy: Ejecutar como YO, Acceso: Cualquiera).
# Copia este archivo a ".env.local" y pega tu URL real.
# En Vercel: Settings > Environment Variables (se hornea en build -> redeploy tras cambiarla).
NEXT_PUBLIC_GAS_URL=https://script.google.com/macros/s/XXXXXXXXXXXXXXXXX/exec
