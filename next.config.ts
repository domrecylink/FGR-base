import type { NextConfig } from 'next'

// App 100% cliente: los datos salen del Web App de Apps Script desde el navegador.
// No hay rutas de servidor propias todavía.
const nextConfig: NextConfig = {}

export default nextConfig
